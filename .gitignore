# Dependencies
node_modules/

# Production builds
dist/

# Vite cache
.vite/

# Logs
*.log

# macOS
.DS_Store

# Environment variables / secrets
.env
.env.*
!.env.example

# Dev Auth repair map (Phase C) — contains UUIDs, not passwords, but keep local
.dev-auth-remap.json

# Supabase local CLI state (never commit)
supabase/.temp/
